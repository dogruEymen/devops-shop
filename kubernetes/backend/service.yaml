apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: devops-shop

spec:
  selector:
    app: backend

  ports:
    - name: http
      port: 80
      targetPort: 8080

  type: ClusterIP