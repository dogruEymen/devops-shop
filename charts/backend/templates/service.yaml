apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: backend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  type: {{ .Values.service.type }}